#math #quantum #group-theory

**The Magnus expansion writes the time-evolution operator of a time-dependent Hamiltonian as a single exponential, $U(t) = e^{\Omega(t)}$, with $\Omega$ built from nested commutators of $H$ at different times.** Unlike the Dyson series it is unitary at every truncation order — you are approximating the *generator*, not the propagator — which is why it underlies average Hamiltonian theory, dynamical decoupling design, and gate constructions.

# Reference

For $i\dot{U} = H(t)U$ (with $\hbar = 1$), write $U(t) = \exp\!\left[\sum_{k=1}^{\infty} \Omega_k(t)\right]$ with

$$\Omega_1 = -i\int_0^t \! dt_1\, H(t_1)$$

$$\Omega_2 = -\frac{1}{2}\int_0^t \! dt_1 \int_0^{t_1}\! dt_2\, [H(t_1), H(t_2)]$$

$$\Omega_3 = \frac{i}{6}\int_0^t\!\! \int_0^{t_1}\!\! \int_0^{t_2} \big( [H_1,[H_2,H_3]] + [H_3,[H_2,H_1]] \big)\, dt_3\, dt_2\, dt_1$$

with $H_i \equiv H(t_i)$. Each $\Omega_k$ is a $k$-fold time-ordered integral of $(k{-}1)$-nested commutators. Key structural facts:

- **Every $\Omega_k$ is anti-Hermitian**, so any truncation $e^{\Omega_1 + \dots + \Omega_n}$ is exactly unitary. The Dyson series truncated at any order is not.
- **$\Omega_k$ lives in the Lie algebra** generated by $\{H(t)\}$ — the expansion never leaves the algebra, so symmetries of $H$ are preserved order by order (see [[Lie Algebras]]).
- If $[H(t_1), H(t_2)] = 0$ for all times, the series terminates at $\Omega_1$: $U = e^{-i\int H\,dt}$, the naive guess. All higher terms are the price of non-commutativity at different times — the same origin as [[Baker-Campbell-Hausdorff]].
- **Convergence** is guaranteed for $\int_0^t \lVert H(t')\rVert\, dt' < \pi$. In practice: short times or weak/fast drives.

**Exact termination — the trapped-ion case.** If $H(t)$ is *linear* in $\hat{x}, \hat{p}$ (or $\hat{a}, \hat{a}^\dagger$), the commutator $[H(t_1), H(t_2)]$ is a c-number, so $\Omega_k = 0$ for $k \geq 3$ and the Magnus expansion is **exact at second order**. This is why the [[Molmer-Sorensen Gate]] admits a closed-form solution: $\Omega_1$ is a spin-dependent displacement (which closes for a full loop in phase space) and $\Omega_2$ is the geometric phase — the enclosed area — that becomes the entangling gate.

**Average Hamiltonian theory (AHT).** For a periodic $H(t)$ with cycle time $T$, define the effective Hamiltonian by $U(T) = e^{-i\bar{H}T}$ and expand $\bar{H} = \bar{H}^{(0)} + \bar{H}^{(1)} + \dots$:

$$\bar{H}^{(0)} = \frac{1}{T}\int_0^T \tilde{H}(t)\, dt, \qquad \bar{H}^{(1)} = -\frac{i}{2T}\int_0^T\!\! dt_1 \int_0^{t_1}\!\! dt_2\, [\tilde{H}(t_1), \tilde{H}(t_2)],$$

where $\tilde{H}$ is the Hamiltonian in the **toggling frame** (the interaction frame of the pulses). Pulse-sequence design is the art of choosing pulses so the unwanted terms of $\bar{H}^{(0)}$ average to zero, then symmetrizing the sequence in time so $\bar{H}^{(1)}$ (all odd orders, in fact) vanish too. [[Spin Echo and Dynamical Decoupling]] sequences (CPMG, XY-8, WAHUHA) are exactly this: the echo cancels the static term at $\bar{H}^{(0)}$; higher-order sequences chase down the residual commutator terms.

**Floquet connection**: for periodic driving, $\bar{H}$ is the Floquet Hamiltonian, and the Magnus series in $1/\omega_{\text{drive}}$ is the standard high-frequency expansion.

> [!question]- Why prefer Magnus over the Dyson series when both are expansions of the same $U(t)$?
> Truncated Dyson violates unitarity — probabilities leak and fake decay/gain appears at long times. Truncated Magnus errs only *within* the group: you get a wrong but perfectly unitary evolution, with symmetries intact. For designing control sequences this is the correct error model: the residual is an effective Hamiltonian you can attack at the next order, not a non-physical loss.

# Connections

- [[Baker-Campbell-Hausdorff]] — the static version: combining exponentials via nested commutators
- [[Lie Algebras]] — the space $\Omega$ lives in; closure under commutators is why symmetry survives truncation
- [[Spin Echo and Dynamical Decoupling]] — AHT in action: sequences engineered to zero out $\bar{H}^{(0)}, \bar{H}^{(1)}$
- [[Molmer-Sorensen Gate]] — Magnus terminating exactly for Hamiltonians linear in phase-space operators
- [[Rotating Wave Approximation]] — keeping only $\bar{H}^{(0)}$ of a fast drive is an RWA-type statement
- [[Trotter Product Formula]] — the complementary decomposition: splitting one exponential into many

---
Source: Blanes et al., "The Magnus expansion and some of its applications," *Phys. Rep.* 470, 151 (2009); Haeberlen & Waugh, *Phys. Rev.* 175, 453 (1968) (AHT)
